-- group by 
-- having 

-- exemplo:
-- SELECT cor, COUNT(*), SUM(campo), AVG(campo), MAX(campo), MIN(campo)
-- FROM botao
-- GROUP BY cor 
-- saida: amarelo -> 1, preto -> 5, branco -> 9, rosa -> 3, pink -> 8, laranja -> 2
-- HAVING : filtrar pelas funções
-- ORDER BY  

-- select idcor, count(*)
-- from veiculos
-- group by idcor
-- having count(*)>2; -- a soma da cor for maior do que 2

 

 